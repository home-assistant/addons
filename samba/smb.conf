[global]
   workgroup = %%WORKGROUP%%
   server string = Samba HomeAssistant config share
   security: user

   load printers = no
   disable spoolss = yes

   log level = 1

   #guest account = root
   #map to guest = Bad User

[config]
   browseable = yes
   writeable = yes
   path = /config
