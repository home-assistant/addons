{
    "logConfig": {
        "enabled": true,
        "level": 4,
        "forceConsole": true
    },
    "storage": {
        "cacheDir": "/data/cache",
        "throttle": "slow"
    },
    "networkKey": "{{ .network_key }}"
}
