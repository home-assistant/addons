diff --git a/src/core/border_router/routing_manager.cpp b/src/core/border_router/routing_manager.cpp
index 6be8b9002..4b2c98d9f 100644
--- a/src/core/border_router/routing_manager.cpp
+++ b/src/core/border_router/routing_manager.cpp
@@ -36,7 +36,7 @@
 #if OPENTHREAD_CONFIG_BORDER_ROUTING_ENABLE
 
 #include <string.h>
-
+#include <arpa/inet.h>
 #include <openthread/border_router.h>
 #include <openthread/platform/border_routing.h>
 #include <openthread/platform/infra_if.h>
@@ -215,26 +215,56 @@ exit:
 void RoutingManager::LoadOrGenerateRandomBrUlaPrefix(void)
 {
     bool generated = false;
+    bool suggested = false;
 
     if (Get<Settings>().Read<Settings::BrUlaPrefix>(mBrUlaPrefix) != kErrorNone || !IsValidBrUlaPrefix(mBrUlaPrefix))
     {
-        Ip6::NetworkPrefix randomUlaPrefix;
+        const char *envSuggested = getenv("OTBR_SUGGESTED_BR_ULA_PREFIX");
+        if (envSuggested != nullptr)
+        {
+            char addrPart[64];
+            int  prefixLen = 0;
+
+            if (sscanf(envSuggested, "%47[^/]/%d", addrPart, &prefixLen) == 2 &&
+                prefixLen == kBrUlaPrefixLength)
+            {
+                struct in6_addr addr6;
+                if (inet_pton(AF_INET6, addrPart, &addr6) == 1)
+                {
+                    mBrUlaPrefix.Clear();
+                    mBrUlaPrefix.mLength = kBrUlaPrefixLength;
+                    memcpy(mBrUlaPrefix.mPrefix.mFields.m8, addr6.s6_addr, 6);
+
+                    if (IsValidBrUlaPrefix(mBrUlaPrefix))
+                    {
+                        Get<Settings>().Save<Settings::BrUlaPrefix>(mBrUlaPrefix);
+                        suggested = true;
+                        LogNote("No valid /48 BR ULA prefix found in settings, using suggested one from command line");
+                    }
+                }
+            }
+        }
+
+        if (!suggested)
+        {
+            Ip6::NetworkPrefix randomUlaPrefix;
 
-        LogNote("No valid /48 BR ULA prefix found in settings, generating new one");
+            LogNote("No valid /48 BR ULA prefix found in settings, generating new one");
 
-        SuccessOrAssert(randomUlaPrefix.GenerateRandomUla());
+            SuccessOrAssert(randomUlaPrefix.GenerateRandomUla());
 
-        mBrUlaPrefix.Set(randomUlaPrefix);
-        mBrUlaPrefix.SetSubnetId(0);
-        mBrUlaPrefix.SetLength(kBrUlaPrefixLength);
+            mBrUlaPrefix.Set(randomUlaPrefix);          // <-- .Set() for this OT commit
+            mBrUlaPrefix.SetSubnetId(0);
+            mBrUlaPrefix.SetLength(kBrUlaPrefixLength);
 
-        Get<Settings>().Save<Settings::BrUlaPrefix>(mBrUlaPrefix);
-        generated = true;
+            Get<Settings>().Save<Settings::BrUlaPrefix>(mBrUlaPrefix);
+            generated = true;
+        }
     }
 
     OT_UNUSED_VARIABLE(generated);
-
-    LogNote("BR ULA prefix: %s (%s)", mBrUlaPrefix.ToString().AsCString(), generated ? "generated" : "loaded");
+    const char *status = suggested ? "suggested" : (generated ? "generated" : "loaded");
+    LogNote("BR ULA prefix: %s (%s)", mBrUlaPrefix.ToString().AsCString(), status);
 }
 
 void RoutingManager::EvaluateState(void)
