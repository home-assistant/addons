{
    "logConfig": {
        "enabled": true,
        "level": {{ if eq .logging "debug" }}5{{ else }}2{{ endif }},
        "forceConsole": true
    },
    "storage": {
        "cacheDir": "/data/cache",
        "throttle": "slow"
    },
    "networkKey": "{{ .network_key }}"
}
